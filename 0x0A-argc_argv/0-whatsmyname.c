#include <stdio.h>

/**
  * main - Prints the name of the file
  * @argc: input
  * @argv: input
  * Return: Always 0
  */

int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
