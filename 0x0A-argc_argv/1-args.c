#include <stdio.h>

/**
  * main - Prints the number of argument
  * @argc: input
  * @argv: input
  * Return: Always 0
  */

int main(int argc, __attribute__((unused)) char *argv[])
{
	printf("%d\n", argc);

	return (0);
}
