#include "main.h"

/**
  * add - Adds two int and return the result
  * @x, @y: Integers to be added
  * Return: the summed value
  */

int add(int x, int y)
{
	int result;

	result = x + y;
	return (result);
}
