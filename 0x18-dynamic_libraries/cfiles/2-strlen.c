#include "main.h"

/**
  * _strlen - Returns the lenght of a string
  * @s: The string to work on
  * Return: void
  */

int _strlen(char *s)
{
	int count;

	for (count = 0; count[s] != '\0'; count++)
	{
	}

	return (count);
}
